# SudoBox 内核模块清单（KSU / Magisk）
#
# id 保留 chroot-mgr：它是 /data/adb/modules/<id> 的目录名与 bin/chroot-mgr 的
# 二进制名，贯穿 module 内部脚本、App 的 MANAGER_PATHS/PATH、数据目录
# /data/adb/chroot-mgr/data。改名 = 全量换路径 + 已装设备全部重刷，收益低于风险。
# 用户可见的"模块名"由 name 字段表达（KSU/Magisk 管理器显示）。
id=chroot-mgr
name=SudoBox Service
version=v0.1.25
versionCode=26
author=SudoBox contributors
description=SudoBox Service — 按需提权，即用即还。v0.1.25：修复容器运行时 status --json 卡死 120s——根因是 du 无 -x 扎进 rootfs 内挂载的 /proc /sys /dev 虚拟文件系统（v0.1.24 首次在运行态暴露），App 3s 轮询叠加成 I/O 风暴；修复 = du -x 不跨文件系统 + size 300s TTL 缓存（.size.cache），轮询路径零成本。v0.1.24：新增全局监督 daemon（chroot-mgr daemon start [--boot]|stop|status）——enable 首容器幂等拉起、末容器 disable 即杀、空转自退兜底（SUDOBOX_DAEMON_IDLE_TICKS/MAX_TICKS），容器结束不留常驻；职责=状态对账 tick + boot-restore 执行者（service.sh 读 config/daemon-mode 时移交，legacy 循环保留）+ 事件日志 daemon.log；boot-restore 的 enable 以子壳隔离 die 防中断；status --json 新增 daemon 字段（App v0.1.32 仪表盘展示）。v0.1.22：修复 App 仪表盘 CPU KPI 恒显示"-"——sepolicy.rule 新增最小精确规则 allow untrusted_app proc_loadavg file { open read getattr }（vivo Android 15 GKI 将 /proc/loadavg 标注为独立类型 proc_loadavg 且未授予 appdomain 读权限，App 进程域直读被 avc 拒、runCatching 吞成 "-"，与容器启停及 root 授权无关；ksud sepolicy 解析器为 magiskpolicy 无冒号形式，规则按此书写，实测 check/patch 通过、策略文件无膨胀、Enforcing 保持、实时注入后 KPI 恢复真实负载显示）。v0.1.21：新增全局 PATH 注入 chroot-mgr path install|remove|status——利用 KSU ksud 每次 su 调用（含 su -c 非交互）把 /data/adb/ksu/bin 前置进 PATH 的机制（ksu.rs add_path_to_env），将 chroot-mgr/sudobox symlink 进该目录，使所有 su 会话免全路径直接调用；symlink 仅在 su 执行瞬间动态生效，完整重启后临时 root 消失即随之失效（与临时 root 同生共死，软重启保留），不写 /system（D11）；customize.sh 安装时自动注入（warn-only 幂等），uninstall.sh 清理时只删指向本模块的链接；同时使 help 文档中 su -c chroot-mgr ... 示例真正可用（此前模块从不装任何 PATH 入口）。v0.1.20：修复升级时 customize.sh 的 chcon -R 在容器挂载期间穿越 rootfs/dev（宿主 /dev 的 bind）把宿主 /dev/null 错标为 adb_data_file、导致软重启后 system_server 崩溃循环卡 Logo 的问题（改为 find -xdev + 剪除 rootfs 子树 + 不跟随符号链接）。v0.1.19：升级路径修复——customize.sh 新增升级前容器对账清理（停守护+卸载活跃挂载，warn-only），消除"升级时容器挂载/守护与新模块并存"的状态脱节；devpts/devshm 不再在宿主命名空间创建/挂载全局 /dev/pts、/dev/shm（改为容器内直挂）。v0.1.18：新增进程守护层——enable/enter 启动链早期把根进程迁出 cgroup v2 uid slice（防 Android 12+ cached app freezer 冻结容器服务，cgroup/oom_score_adj 随 fork 继承、迁根即迁全树，顶层写入失败降级专建子组再失败 warn-only，SUDOBOX_NO_KEEPALIVE=1 可关）+ oom_score_adj=-1000 托底 + watchdog 守护循环（setsid 双重 fork 脱离 App 谱系，巡检 /etc/init.d/<svc> status 崩溃自动重启，pidfile 三重自检防孤儿，disable 首步停守护防拉活竞争，status --json 新增 guard 字段，guard 命令手动控制，GUARD_SERVICES 可自定义服务清单）。v0.1.17：修复在线安装"无法获取连接"——alpine DEFAULT_SUITE 由已 EOL 的 v3.21 改为 latest-stable 指针（四镜像实测 200）；ubuntu resolver 重写（镜像前缀静默失效 + 文件名缺补丁版本号 404 两 bug，改为目录列表动态解析，TUNA/USTC/阿里云 ubuntu-cdimage 路径实测可达）；kali 文件名 kalifs-* → kali-nethunter-rootfs-full-*（旧名实测 404）并新增动态 resolver（NetHunter rootfs 无国内镜像，非官方源明确警告）。unshare -m + chroot 引擎的移动 GNU/Linux 工作台控制面（容器启停/发行版安装导入/终端）。v0.1.16：修复状态漂移与会话残留——状态文件只在操作边界写入、会话自行退出后卡死 running 导致 App 误报"已有系统在运行"；新增 cm_state_reconcile 以进程台账+挂载实况对账（status/enable 前调用）：无挂载自愈 disabled、有挂载无进程降级 enabled；switch 新增 --no-enter（仲裁停旧→卸载→启用新后返回，不拉交互 shell，App 启动/切换专用，此前落点 enter 在无 tty 的 su -c 里假挂到超时）。v0.1.15：修复容器内 sudo/setuid 全部失效——/data 的 nosuid 是 per-mount 标志而非 superblock 属性，将 rootfs bind mount 到自身得到标志独立的新挂载后 remount,exec,suid,dev 即可安全打开 suid（linuxdeploy 十年验证机制）：不动宿主 /data 挂载标志（杜绝全局 remount 的 panic 风险）、disable 随 umount_all 逆序卸载零残留、remount 失败 warn-only 不阻断容器、SUDOBOX_NO_SUID=1 可整体关闭。v0.1.14：新增 root 专用入口 chroot-mgr root [profile|--name ID] [-- cmd...]——任何容器以 root 进入：跳过首次用户配置（运维场景不应被非 root 用户的首次 prompt 打断），SUDOBOX_FORCE_ROOT=1 使 cm_enter_session 不导出已配置用户名、chroot-enter.sh 整段跳过非 root 分支，bash -l 直落 root shell（不经 setpriv/su/PAM）；enter 保持原行为（配置过则落非 root 用户）。v0.1.13 既有：修"配置成功但进入后静默秒退"真根因——mksh/POSIX 非交互 shell 异步列表（cmd &）默认 stdin→/dev/null，后台任务显式 </dev/tty + chroot-enter.sh 兜底恢复 tty。v0.1.12 既有：setpriv 直降权去 su/PAM。v0.1.11 既有：预置同名账户幂等修复。v0.1.10 既有：假成功禁令。v0.1.9 既有：首次进入配置非 root 用户 + user-config 子命令。v0.1.8/0.1.7/0.1.6/0.1.5/0.1.4/0.1.2/0.1.3 既有：详见 CHANGELOG。
updateJson=
